const mongoose = require('mongoose'); const User = require('./src/models/User'); mongoose.connect('mongodb+srv://sameer:sameer@cluster0.0uf3e89.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0').then(async () => { const users = await User.find({}).limit(3); console.log(JSON.stringify(users.map(u => ({id: u._id, phone: u.phone, sub: u.subscription, name: u.name})), null, 2)); process.exit(0); });
